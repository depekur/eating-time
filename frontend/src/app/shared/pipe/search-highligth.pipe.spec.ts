import { SearchHighligthPipe } from './search-highligth.pipe';

describe('SearchHighligthPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHighligthPipe();
    expect(pipe).toBeTruthy();
  });
});

import { WordEndingsPipe } from './word-endings.pipe';

describe('WordEndingsPipe', () => {
  it('create an instance', () => {
    const pipe = new WordEndingsPipe();
    expect(pipe).toBeTruthy();
  });
});
